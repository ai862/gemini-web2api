{
  "name": "gemini-web2api-worker",
  "version": "1.1.0",
  "private": true,
  "description": "Gemini Web to OpenAI API — Cloudflare Workers port",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
